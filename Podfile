platform :ios, '10.3'
inhibit_all_warnings!

target 'Talky' do
    use_frameworks!
    
    target 'TalkyTests' do
        inherit! :search_paths
        
        pod 'Quick', '~> 1.0'
        pod 'Nimble', '~> 7.0'
    end
    
end
